/data/log/nginx/*.log {
        daily
        missingok
        rotate 7
        compress
        notifempty
        create 640 nginx adm
        sharedscripts
        postrotate
                [ -f /opt/run//nginx.pid ] && kill -USR1 `cat /opt/run//nginx.pid`
        endscript
}
